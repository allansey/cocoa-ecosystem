export const metadata = {
  title: 'Dashboard',
  description: 'Manage your CocoaLink account and listings',
};

export default function DashboardLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
