export const metadata = {
  title: 'Register',
  description: 'Create a new CocoaLink account as a Farmer or Buyer',
};

export default function RegisterLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
